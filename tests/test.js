const assert = require('assert');

describe('Simple Node.js App', function() {
    it('should return Hello, World! when accessing root URL', function() {
        assert.equal('Hello, World!', 'Hello, World!');
    });
});

